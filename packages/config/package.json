{
    "name": "config",
    "version": "0.0.0",
    "main": "index.ts",
    "license": "MIT",
    "files": [
      "amplify",
      "auth"
    ],
    "scripts": {
      "build": "echo 'No build required for config package'",
      "lint": "echo 'No lint required for config package'"
    },
    "dependencies": {
      "aws-amplify": "^6.10.0"
    }
  }