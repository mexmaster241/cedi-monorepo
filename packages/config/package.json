{
  "name": "config",
  "version": "0.0.0",
  "main": "index.ts",
  "types": "index.ts",
  "license": "MIT",
  "files": [
    "index.ts",
    "amplify",
    "auth"
  ],
  "scripts": {
    "build": "tsc --noEmit",
    "clean": "rm -rf .turbo"
  },
  "dependencies": {
    "aws-amplify": "^6.10.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}